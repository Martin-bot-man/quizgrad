import React from 'react'

const Button = ({name, onClick, children, ...props}) => {
  return (
  <button className="bg-[rgba(252,200,34,1)] cursor-pointer w-5 h-6 " name ={FIGMA}>

  </button>

      
  );
}

export default Button